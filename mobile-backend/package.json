{
	"name": "mobile-backend",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"dev": "nodemon",
		"build": "tsc -p .",
		"prepare-dist": "npm run build && mkdir -p dist && cp package.json dist/ && cp package-lock.json dist/ && cp -r src/middlewares dist/middlewares && cp -r src/routes dist/routes && cp -r src/utils dist/utils",
		"test": "npm run prepare-dist && cp -r node_modules dist/node_modules",
		"install-prod-deps": "cd dist && npm install --only=production"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@aws-sdk/client-cognito-identity-provider": "^3.529.0",
		"@aws-sdk/client-sns": "^3.620.1",
		"cors": "^2.8.5",
		"dotenv": "^16.4.7",
		"express": "^4.19.2",
		"mysql2": "^3.9.6",
		"serverless-http": "^3.2.0"
	},
	"devDependencies": {
		"@types/cors": "^2.8.17",
		"@types/express": "^4.17.21",
		"nodemon": "^3.1.0",
		"rimraf": "^5.0.5",
		"typescript": "^5.4.5"
	}
}
